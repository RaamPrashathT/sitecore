import Provision from "@/features/auth/components/Provision";

const ProvisionPage = () => {
    return (
        <div>
            <Provision />
        </div>
    );
};

export default ProvisionPage;
